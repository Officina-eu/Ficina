{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the alomails window. The window hosts our own trusted origin (mail.alomails.com) and calls no Rust commands, so only the core defaults are granted — no filesystem, shell, or other native APIs are exposed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
