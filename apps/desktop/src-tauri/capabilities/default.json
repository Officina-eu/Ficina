{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the alomails window. The UI is bundled locally and calls no Rust commands beyond the native HTTP client, which is scoped to the hosted alo API host only — no filesystem, shell, or other native APIs are exposed.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "https://mail.alomails.com/*" }]
    },
    "updater:default",
    "process:allow-restart"
  ]
}
