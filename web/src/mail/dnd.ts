// The dataTransfer MIME type used when dragging a message onto a folder.
// A custom type (not text/plain) so only Ficina's own drops are recognized.
export const DRAG_EMAIL_MIME = "application/x-ficina-email";
