[package]
name = "ficina-smtp-client"
description = "Ficina outbound SMTP client and reply/line parsing, shared by the SMTP delivery path and the JMAP submission path"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
tokio = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[lints]
workspace = true
